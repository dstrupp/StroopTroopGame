{
  "name": "stroop-troop-pickem",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "esbuild src/app.jsx --bundle --minify --outfile=public/app.js --define:process.env.NODE_ENV=\\\"production\\\"",
    "dev": "netlify dev"
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
