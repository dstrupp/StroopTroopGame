{
  "name": "stroop-troop-pickem",
  "version": "1.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node setup.mjs",
    "bundle": "esbuild app.jsx --bundle --minify --outfile=app.js --define:process.env.NODE_ENV=\\\"production\\\""
  },
  "dependencies": {
    "@netlify/blobs": "^11.0.3",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
